# PixelmonPokedexRewards

Provides rewards to players for reaching milestones on Pokedex completion.

The `/pokedexrewards` command is used to claim rewards.

The `/pokedextop` command is used to compare the Pokedex status of the top players.

The `/pokedex` command is used to check Pokedex status.